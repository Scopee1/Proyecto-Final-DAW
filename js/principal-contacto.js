'use strict';
// Inicializa los modulos de la pagina de contacto una vez construido el DOM.
function iniciarPaginaDeContacto() {
    iniciarTema();
    iniciarModales();
    iniciarContacto();
}
window.addEventListener('DOMContentLoaded', iniciarPaginaDeContacto);
